class MatchManagerT01 is subclass of TestCase, CommonType, MyPrintTestListener

instance variables

man : MatchManager := new MatchManager();




operations 

public MatchManagerT01 : () ==> MatchManagerT01
MatchManagerT01() == TestCase("T01");

public MatchManagerT01 : seq of char ==> MatchManagerT01
MatchManagerT01(str) == TestCase(str);

public setUp : () ==> ()
setUp() == (
		println("\n------------------Group-----------------");
		man.initGroup(4);
		man.calc();
		man.printAll();
);

public runTest : () ==> ()
runTest() == 
  (
  
		dcl matchmakes : map Match to MatchMake := man.getAllMatchMakes();
		dcl matchmake : MatchMake := matchmakes(mk_(2,1));
		println("------------------START-----------------");
--		for all matchmake in set rng man.getAllMatchMakes() do (
--			matchmake.setScore()
--		);
		matchmake.setScore(3,2);
		man.calc();
		man.printAll();
	);

public tearDown : () ==> ()
tearDown() == 
  skip;


end MatchManagerT01